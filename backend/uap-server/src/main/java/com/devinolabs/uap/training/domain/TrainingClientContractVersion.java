package com.devinolabs.uap.training.domain;

/**
 * Aggregate client response contract for training facades.
 * Distinct from HTTP route versioning ({@code /api/v1}).
 */
public enum TrainingClientContractVersion {
	V1
}
