# development | staging | production
VITE_UAP_ENV=development

# Optional in development (defaults to same-origin via Vite /api proxy).
# Required for staging and production.
# VITE_UAP_API_BASE_URL=https://api.example.com
