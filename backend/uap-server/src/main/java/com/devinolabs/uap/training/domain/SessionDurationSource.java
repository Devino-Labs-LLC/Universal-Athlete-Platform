package com.devinolabs.uap.training.domain;

/**
 * How session duration minutes were resolved for an effort or load summary.
 */
public enum SessionDurationSource {

	ATHLETE_REPORTED,
	OCCURRENCE_TIMESTAMPS,
	UNKNOWN

}
